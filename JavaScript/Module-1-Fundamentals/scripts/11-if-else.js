/*
=> CONDITIONALS:

- If conditional === true SO do one thing ELSE do something else
- If and Else:
if (conditional === true) {
   code block
} else {
   code block
}
- Ternary Operator:
   const result = conditional ? result_true : result_false

   const result = (6 === 6) ? "TRUE" : "FALSE"
*/