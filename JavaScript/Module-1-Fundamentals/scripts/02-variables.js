/*
VARIABLES:

=> Serve for store data so we can use them in our code
=> How to create a variable:
   - var = older and not currently used
   - let = newer and it's possible to change the data inside it (let it change)
   - const = the value inside it doesn't change
=> How to name a variable:
   - cases: camel case (userName), snake case (user_name), etc
   - significant names
*/