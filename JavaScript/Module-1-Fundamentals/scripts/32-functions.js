/*
FUNCTIONS:

=> It's a framework Javascript that allows you to declare a procedure and run it programmatically
=> A function can be sen as: inputs -> procedure -> outputs
=> The outputs and the inputs are optionals
=> The function inputs are called parameters and the functions outputs are returns
=> Syntax:
   function function_name(inputs) {
      // procedures
      return outputs
   }
*/

function hello() {
   console.log("Hello World!")
}

hello()