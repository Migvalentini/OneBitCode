/*
TEMPLATE LITERALS:

=> Another way to declare strings
=> Use the crasis
=> Allows strings interpolation and automatic break line
=> Example:
*/

let string = `This row will be
broken automatically
    and this one will brake and will have a space at the beginning
Expression result: ${2 + 2}`

const name = 'Miguel'

console.log(string)
console.log(`Hello ${name}`)